{
  "name": "booknest-onepage",
  "private": true,
  "scripts": {
    "dev": "http-server full -p 4173 -c-1",
    "test:e2e": "playwright test",
    "test:ci": "playwright install --with-deps && playwright test",
    "check:forbidden": "./scripts/check-forbidden-terms.sh",
    "lint": "npm run check:forbidden"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.47.0",
    "@types/node": "^24.3.0",
    "http-server": "^14.1.1",
    "playwright": "^1.47.0"
  }
}
