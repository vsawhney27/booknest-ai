# BookNest AI — Minimal Environment (MVP)

# --- App ---
APP_BASE_URL=https://your-domain.com
ENABLE_LIVE_INTEGRATIONS=false

# --- Email notifications (required; server only) ---
EMAIL_PROVIDER_API_KEY=

# --- Notification routing (non-secret; server only) ---
NOTIFY_SALES_EMAIL=sales@your-domain.com
NOTIFY_SUPPORT_EMAIL=support@your-domain.com

# --- Anti-spam (required) ---
HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET=

# --- Slack notifications (optional — choose ONE approach) ---
# A) Simple webhook (recommended for MVP)
SLACK_WEBHOOK_URL=
# B) Bot (advanced; comment out if using webhook)
# SLACK_BOT_TOKEN=
# SLACK_SIGNING_SECRET=
# SLACK_DEFAULT_CHANNEL_ID=

# --- Optional integrations (leave commented until you actually use them) ---
# Google Calendar API (only if creating events programmatically)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=
# GOOGLE_REFRESH_TOKEN=
# Microsoft 365 Calendar API (only if creating events programmatically)
# MS_CLIENT_ID=
# MS_CLIENT_SECRET=
# MS_TENANT_ID=
# MS_REDIRECT_URI=
# Zendesk (only if auto-ticketing)
# ZENDESK_SUBDOMAIN=
# ZENDESK_EMAIL=
# ZENDESK_API_TOKEN=